# Version `1.2.0`
- More configurable features
- Random comments
- Bugfixes
- We now have a changelog
- Version is now checked automatically
- Booleans are now obfuscated
- Redo feature
- the `v` flag is now only for verbose, use `--version` for version